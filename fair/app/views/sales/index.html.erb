<p id="notice"><%= notice %></p>

<h1>Listing Sales</h1>

<dif class="table-responsive">
<table class="table table-striped table-hover table-responsive">
  <thead>
    <tr>
      <th>Data Inicio</th>
      <th>Data Fechamento</th>
      <th>Status</th>
      <th>Comentários</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sales.each do |sale| %>
      <tr>
        <td><%= sale.nome %></td>
        <td><%= sale.tipo %></td>
        <td><%= sale.grouping %></td>
        <td><%= sale.inf %></td>
        <td><%= sale.coments %></td>
        <td><%= link_to 'Show', sale %></td>
        <td><%= link_to 'Edit', edit_complement_path(sale) %></td>
        <td><%= link_to 'Destroy', sale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</dif>
<br>

<%= link_to 'New Sales', new_sale_path %>
